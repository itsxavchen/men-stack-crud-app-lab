<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create a dog record</title>
  </head>
  <body>
    <h1>Edit <%= dog.name %></h1>
    <form action="/dogs/<%=dog._id%>?_method=PUT" method="POST">
        <label for="name">Name:</label>
        <input type="text" name="name" value="<%= dog.name %>" />
        <br>
        <br>
        <label for="breed">Breed:</label>
        <input type="text" name="breed" value="<%= dog.breed %>" />
        <br>
        <br>
        <label for="sex">Sex:</label>
        <input type="text" name="sex" value="<%= dog.sex %>" />
        <br>
        <br>
        <label for="isDewormed">Dewormed?</label>
        <input type="checkbox" name="isDewormed" <% if (dog.isDewormed) { %>checked<% } %> />
        <br>
        <br>
        <button type="submit">Update this record</button>
      </form>
      <a href="/dogs/">Back to view all registered dogs</a>
  </body>
</html>